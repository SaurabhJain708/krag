{
  "name": "@repo/db",
  "version": "0.0.1",
  "scripts": {
    "db:generate": "npx dotenv-cli -e ../../.env -- npx prisma@5.17.0 generate --generator client && cd ../../apps/ingestion-worker && uv run prisma generate --schema=../../packages/database/prisma/schema.prisma --generator client_py && cd ../../apps/retrieval-worker && uv run prisma generate --schema=../../packages/database/prisma/schema.prisma --generator client_py_retrieval",
    "db:migrate": "npx dotenv-cli -e ../../.env -- prisma migrate dev",
    "db:deploy": "npx dotenv-cli -e ../../.env -- prisma migrate deploy"
  },
  "devDependencies": {
    "@types/node": "^22.19.7",
    "@types/pg": "^8.16.0",
    "prisma": "5.17.0"
  },
  "dependencies": {
    "@prisma/adapter-pg": "5.17.0",
    "@prisma/client": "5.17.0",
    "pg": "^8.17.2"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
